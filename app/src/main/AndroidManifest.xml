<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.anroid_quiz_game">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TrueOrFalseActivity"
            android:screenOrientation="userLandscape">
        </activity>
        <activity android:name=".TheOtherMeActivity"
            android:screenOrientation="userLandscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TFLevelActivity" />
        <activity android:name=".TalkToMeActivity" />
        <activity android:name=".TalkLevelActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".OtherLevelActivity" />
        <activity android:name=".HighScoresActivity" />
        <activity android:name=".GameResultsActivity" />
        <activity android:name=".CategoriesActivity" />
    </application>

</manifest>